#!/bin/bash
set -euf -o pipefail

##
# Generate a random XID string.
#
# Usage:
#
#     random-xid
#
# Example:
#
#     $ random-xid
#     902dba9dd7baf8f67038472d75d232e7
#
# The XID specification is 32 hexdecimal characters, lowercase,
# and randomly generated by a secure random generator.
#
# This script uses the commands `hexdump` and `/dev/random`
# to generate secure random numbers and format them.
#
# Author: Joel Parker Henderson (joel@joelparkerhenderson.com)
# License: GPL
# Updated: 2015-01-25
##

hexdump -n 16 -v -e '16/1 "%02x" "\n"' /dev/random
